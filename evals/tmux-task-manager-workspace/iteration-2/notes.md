# Iteration 2 notes

## Why rerun
The skill was reframed from a tmux-centric description toward a background task management skill whose underlying runtime happens to be tmux.

## Eval updates
- prompts now talk about background tasks more than tmux itself
- expectations now prefer task-management language
- rerun eval expects reuse of the same task slot when practical
- disappeared eval expects the agent to explain the higher-level task-slot abstraction while still knowing the low-level window semantics

## Configs
- old-skill: snapshot from iteration-1 baseline
- new-skill: current skills/tmux-task-manager/SKILL.md
